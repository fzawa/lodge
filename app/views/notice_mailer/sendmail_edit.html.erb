<!DOCTYPE html>
<html>

<head>
<title></title>
<meta charset="utf-8">
</head>

<body>
<p>こんにちは。Lodgeです！<br><b><%= @user.name %></b>さんが記事を編集しました。</p>

<table>
  <tr>
    <td>タイトル</td>
    <td>: <%= @article.title %></td>
  </tr>
  <tr>
    <td>タグ</td>
    <td>: <%= @article.tag_list %></td>
  </tr>
  <tr>
    <td>詳細</td>
    <td>: <a href="<%= url_for :host => ENV['LODGE_DOMAIN'], :controller => :articles, :action => 'show', :id => @article.id %>"><%= url_for :host => ENV['LODGE_DOMAIN'], :controller => :articles, :action => 'show', :id => @article.id %></a></td>
  </tr>
  <tr>
    <td>diff</td>
    <td>: <a href="<%= url_for :host => ENV['LODGE_DOMAIN'], :controller => :update_histories, :action => 'show', :id => @update_history.id %>"><%= url_for :host => ENV['LODGE_DOMAIN'], :controller => :update_histories, :action => 'show', :id => @update_history.id %></a></td>
  </tr>
</table>

</body>
</html>
